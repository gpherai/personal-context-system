// Test stub: `server-only` throws when imported outside a React Server
// environment. Vitest aliases the package to this no-op so server modules
// (which legitimately import "server-only") can be unit-tested.
export {};
